# 导入所有数据库模型
from app.db.models import OAuthAccount  # 从统一的models中导入所有模型
from app.db.models import LimitType, ProviderStats, TranslationProvider, User

# 可以在这里添加更多模型的导入
# 例如：from app.db.models import Post, Comment 等
