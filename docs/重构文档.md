# EPUB 翻译系统重构文档

## 目标

实现**树状递归分块 + 语义合并**的翻译架构：
- 按 HTML 树状结构递归分块，保证标签对完整
- 贪心合并符合 token 限制的最大子标签节点
- 通过 xpath 记录位置信息，验证翻译后结构完整
- Manifest 持久化支持断点续传

---

## 核心架构

```
┌─────────────────────────────────────────────────────────────┐
│                        解析阶段                             │
│  HTML → 树状解析 → 递归分块 → Chunk列表（带 xpath）        │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      持久化状态                             │
│  加载 Manifest（如存在）→ 恢复进度 → 跳过已完成 Chunk       │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      翻译循环                               │
│  for chunk in chunks:                                       │
│    if COMPLETED: continue                                   │
│    translate(chunk) → 验证标签完整性                        │
│    save Manifest（每完成一个即持久化）                       │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      重建阶段                               │
│  按 chunks 顺序拼接 → 输出完整 HTML                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 核心数据结构

### ChunkState

```python
@dataclass
class ChunkState:
    chunk_id: str                      # hash(xpath + original_html[:100])，用于校验
    xpath: str                         # 在文档中的唯一路径，如 "html/body/div[0]"
    original_html: str                 # 原始 HTML 子树（完整标签树）

    status: ChunkStatus                # PENDING | COMPLETED | FAILED
    translated_html: str | None        # 翻译后 HTML

    token_count: int                  # 估算 token 数
    error: str | None                  # 失败原因

    # 翻译内容
    texts: list[str]                   # 需要翻译的纯文本列表
    text_positions: list[dict]         # 每个文本的位置信息
    context_before: str | None         # 前一个 Chunk 的上下文（不翻译）
    context_after: str | None          # 后一个 Chunk 的上下文（不翻译）

    # 时间戳
    created_at: str
    updated_at: str
    completed_at: str | None
```

### TranslationManifest

```python
@dataclass
class TranslationManifest:
    doc_id: str                        # 文档唯一标识
    file_path: str                     # 原始文件路径
    total_tokens: int                  # 文档总 token 数

    chunks: list[ChunkState]
    version: str = "1.0"              # 格式版本，用于迁移

    created_at: str
    updated_at: str
```

### TranslationRequest / Response

**核心原则**：translator agent 仍然处理**整段含标签文本**（与当前方式一致），不需要传递 xpath 信息。

```python
# LLM 翻译请求（与当前方式一致）
class TranslationRequest:
    xpath: str                         # Chunk 根节点的绝对 xpath
    html: str                          # 原始 HTML（含标签，供 LLM 参考结构）
    texts: list[str]                  # 需要翻译的纯文本列表
    positions: list[dict]             # 每个文本的绝对 xpath 位置
    context_before: str | None         # 上下文（不翻译）
    context_after: str | None
    glossaries: dict[str, str]        # 术语表

# positions 格式（绝对 xpath）
# 注意：xpath 仅用于记录位置，不传给 LLM
# 绝对 xpath 格式: "html/body/div[0]/p[0]:text:0"
#   - html/body/div[0]/p[0] = 路径到目标节点
#   - :text = 节点类型（text 或 attr）
#   - :0 = 该节点下的第几个文本子节点

# LLM 翻译响应（与当前方式一致）
class TranslationResponse:
    translation: str                   # 翻译后的整段文本（含标签）
    warnings: list[str] | None        # 警告信息
```

---

## 分块算法

### 核心原则

1. **递归遍历树**：按节点递归，不是按 token 硬切
2. **贪心合并**：尽可能合并子节点直到 token 达限
3. **语义边界**：只在闭合标签处分割，不在标签中间切
4. **单个节点超限则递归**：继续处理它的子节点

### 算法伪代码

```
function chunk_node(node, current_tokens, current_nodes):
    node_tokens = estimate_tokens(node)

    if current_tokens + node_tokens <= SAFE_LIMIT:
        # 可以加入当前 Chunk
        current_nodes.append(node)
        return current_tokens + node_tokens, current_nodes
    else:
        # 保存当前 Chunk
        if current_nodes is not empty:
            yield create_chunk(current_nodes)

        # 如果单个节点就超限，递归处理子节点
        if node_tokens > SAFE_LIMIT:
            if is_leaf_node(node):
                # 极端情况：单个文本节点超限
                # 需要拆分文本节点
                yield from split_text_node(node)
                return 0, []
            else:
                result = []
                for child in node.children:
                    result.extend(chunk_node(child, 0, []))
                return 0, result  # 递归子节点不累积到当前 chunk
        else:
            # 重置，开始新 Chunk
            return chunk_node(node, 0, [node])

function chunk_tree(root):
    chunks = []
    for child in root.children:
        tokens, nodes = chunk_node(child, 0, [])
        if nodes:
            chunks.extend(create_chunk(nodes))
    return chunks
```

### 分块示例

```
<div>                          ← 根节点，超限
  ├── <p>第1段</p>             ← 开始累积
  ├── <p>第2段</p>             ← 累积后 token=600，可接受
  ├── <p>第3段</p>             ← 累积后 token=900，可接受
  ├── <p>第4段</p>             ← 累积后 token=1200，超限！
  │                            ← → 前3个<p>合为一个 Chunk
  ├── <p>第5段</p>             ← 重新开始累积，token=300，可接受
  └── <div>超长内容...</div>   ← 本身超限 → 递归处理
```

**结果**：
```
Chunk 1: <div><p>第1段</p><p>第2段</p><p>第3段</p></div>
Chunk 2: <div><p>第4段</p><p>第5段</p></div>
Chunk 3: <div>超长内容...</div>  （递归处理，内部可能再分）
```

---

## Token 计算

### 安全边界

```python
MAX_TOKEN_LIMIT = 1000
SAFE_LIMIT = int(MAX_TOKEN_LIMIT * 0.85)  # 预留 15% 余量
```

### 估算函数

```python
def estimate_tokens(text: str) -> int:
    """估算 token 数（使用 tiktoken）"""
    try:
        tokenizer = tiktoken.encoding_for_model("gpt-3.5-turbo")
    except KeyError:
        tokenizer = tiktoken.get_encoding("cl100k_base")
    return len(tokenizer.encode(text))
```

### 超长文本节点处理

```python
MAX_TEXT_NODE_TOKENS = 500

def split_text_node(text_node):
    """超长文本节点需要拆分"""
    words = text_node.text.split()
    chunks = []
    current = []
    current_tokens = 0

    for word in words:
        word_tokens = estimate_tokens(word)
        if current_tokens + word_tokens > MAX_TEXT_NODE_TOKENS:
            chunks.append(" ".join(current))
            current = [word]
            current_tokens = word_tokens
        else:
            current.append(word)
            current_tokens += word_tokens

    if current:
        chunks.append(" ".join(current))
    return chunks
```

---

## xpath 位置记录

### 位置标识格式

xpath 用于记录每个文本节点的位置，供验证使用。

```
格式: "html/body/div[0]/p[0]:text:0"
说明:
  - html/body/div[0]/p[0] = 绝对路径到目标节点
  - :text = 节点类型（text 或 attr）
  - :0 = 该节点下的第几个文本子节点
```

### xpath 格式校验

**注意**：以下 import 应放在文件顶部

```python
import re

XPATH_PATTERN = re.compile(r'^([a-zA-Z][a-zA-Z0-9]*)(\[\d+\])*(:(text|attr))?:\d+$')

def is_valid_xpath(xpath: str) -> bool:
    """校验 xpath 格式是否合法"""
    return XPATH_PATTERN.match(xpath) is not None
```

---

## 上下文传递

### 目的

解决 Chunk 边界处上下文丢失导致的翻译不连贯问题。

### 实现

```python
# 构建 Chunk 时设置上下文（使用原始文本，非翻译后文本）
for i, chunk in enumerate(chunks):
    if i > 0:
        # 前一个 Chunk 的最后一个原始文本节点
        prev_texts = chunks[i-1].texts
        chunk.context_before = prev_texts[-1] if prev_texts else None

    if i < len(chunks) - 1:
        # 后一个 Chunk 的第一个原始文本节点
        next_texts = chunks[i+1].texts
        chunk.context_after = next_texts[0] if next_texts else None
```

**注意**：`context_before` 和 `context_after` 使用的是**原始文本**，不是翻译后的文本。目的是让 LLM 在翻译时能了解上下文，但不需要翻译上下文本身。

---

## 持久化与断点续传

### Manifest 文件结构

```json
{
  "doc_id": "Chapter_1.xhtml",
  "file_path": "OEBPS/Text/Chapter_1.xhtml",
  "total_tokens": 15000,

  "stats": {
    "total": 15,
    "completed": 8,
    "pending": 7,
    "failed": 0
  },

  "version": "1.0",
  "created_at": "2026-04-06T10:00:00",
  "updated_at": "2026-04-06T10:30:00",

  "chunks": [
    {
      "chunk_id": "a1b2c3d4",
      "xpath": "html/body/div[0]",

      "original_html": "<div><p>Hello</p><p>World</p></div>",
      "translated_html": "<div><p>你好</p><p>世界</p></div>",

      "status": "COMPLETED",
      "token_count": 150,
      "error": null,

      "texts": ["Hello", "World"],
      "text_positions": [
        {"xpath": "html/body/div[0]/p[0]:text:0", "original": "Hello"},
        {"xpath": "html/body/div[0]/p[1]:text:0", "original": "World"}
      ],
      "context_before": null,
      "context_after": "Next chunk's first text",

      "created_at": "...",
      "updated_at": "...",
      "completed_at": "..."
    }
  ]
}
```

### 原子写入

**注意**：import 应放在文件顶部

```python
import tempfile
import atomicwrites
import os

def save_manifest(manifest):
    """原子写入，避免写入中途崩溃导致文件损坏"""
    temp_path = manifest.path + ".tmp"
    with atomicwrites.atomic_write(temp_path, overwrite=True) as f:
        f.write(json.dumps(manifest.to_dict(), indent=2))
    os.replace(temp_path, manifest.path)
```

### 断点续传流程

```
1. 解析 HTML → 生成 chunks

2. 加载 manifest（如果存在）：
   for each chunk in manifest:
       current_hash = hash(chunk.xpath + current_original_html)
       if current_hash != chunk.chunk_id:
           # 原文已变，标记为 PENDING
           chunk.status = PENDING
           chunk.translated_html = None

3. 遍历 chunks：
   if chunk.status == COMPLETED:
       continue  # 跳过已完成的
   else:
       chunk.status = PENDING
       translate(chunk)
       save_manifest()  # 每完成一个立即持久化
```

---

## 翻译工作流

### 单个 Chunk 翻译流程

**核心原则**：translator agent 接收整段含标签文本，返回翻译后的整段文本（与当前方式一致）。

```
1. 构建 TranslationRequest
   - html: 原始 HTML（含标签，整段文本）
   - context_before/after: 上下文
   - glossaries: 术语表

2. 调用 LLM 翻译
   - translator agent 接收整段含标签文本
   - 返回翻译后的整段文本
   - **与当前方式完全一致**，不需要改变 prompt

3. 验证响应
   - 验证 HTML 标签是否完整配对
   - 验证翻译文本不为空

4. 更新 ChunkState
   - status = COMPLETED
   - translated_html = 翻译后的 HTML
   - completed_at = 当前时间
```

### text_positions 的作用

`text_positions` 记录每个文本节点的位置信息，用于：
1. **验证目的**：确保翻译后 HTML 结构未改变
2. **精确定位**：如需对特定文本进行操作

**格式**：
```python
text_positions = [
    {"xpath": "html/body/div[0]/p[0]:text:0", "original": "Hello"},
    {"xpath": "html/body/div[0]/p[1]:text:0", "original": "World"}
]
```

### Translator Agent Prompt

**不需要修改**！当前 translator prompt 仍然适用，处理整段含标签文本。

### 验证响应格式

```python
def validate_translation_response(translated_html: str, original_html: str) -> bool:
    """验证翻译后的 HTML

    1. HTML 标签是否完整配对
    2. 标签数量是否与原始一致

    注意：新架构验证的是整段 HTML，不是逐个文本
    """
    # 解析翻译后的 HTML
    try:
        translated_tree = parse_html(translated_html)
        original_tree = parse_html(original_html)
    except Exception:
        raise TranslationFormatError("Invalid HTML after translation")

    # 验证标签配对
    if not html_tags_match(original_tree, translated_tree):
        raise TranslationFormatError("HTML tag mismatch after translation")

    return True
```

---

## Proofer 校对

### 当前问题

当前 proofer 使用 `[idN]` 占位符定位校对位置。新架构没有占位符了。

### 解决方案

**proofer 也接收整段含标签文本，返回校对后的整段文本**（与 translator 方式一致）。

```
输入：translated_html（含标签）
输出：proofreaded_html（含标签）
```

**不需要修改 proofer prompt**，它处理整段文本的方式与 translator 相同。

### 验证

校对后的 HTML 仍需验证标签完整性。

---

## Nav/Toc 文件处理

### 特殊性

- `<navPoint>` 是核心语义单元
- `<text>` 是唯一需要翻译的文本
- `<content src="..."/>` 是链接，不需要翻译但要保留

### Chunk 结构

**注意**：Nav 文件的 xpath 也是绝对路径，格式与普通 HTML 一致。

```python
class NavChunk:
    xpath: str  # 指向 navPoint 的绝对路径，如 "html/nav/navMap/navPoint[0]"

    # 可翻译部分（使用绝对路径）
    texts_to_translate = [
        {"xpath": "html/nav/navMap/navPoint[0]/navLabel[0]/text[0]:text:0", "text": "Chapter 1"}
    ]

    # 需要保留的非翻译部分
    preserved = {
        "content_src": "Chapter1.xhtml"
    }
```

### 处理流程

Nav/Toc 文件使用相同的递归分块算法，但节点类型不同：

```
<navMap>
  <navPoint id="np1">                    ← navPoint 1，token=100，可累积
    <navLabel><text>Chapter 1</text></navLabel>
    <content src="Chapter1.xhtml"/>
  </navPoint>
  <navPoint id="np2">                    ← navPoint 2，token=100，累积200，可接受
    <navLabel><text>Chapter 2</text></navLabel>
    <content src="Chapter2.xhtml"/>
  </navPoint>
  <navPoint id="np3">                    ← navPoint 3，token=100，累积300，超限！
    <navLabel><text>Chapter 3</text></navLabel>
    <content src="Chapter3.xhtml"/>
  </navPoint>
</navMap>
```

**结果**：
```
Chunk 1: <navMap> 包含 navPoint 1 和 2
Chunk 2: <navPoint id="np3"> 整体
```

**注意**：`content` 元素不需要翻译，但在遍历时需要保留其 `src` 属性。`text` 元素的内容是唯一需要翻译的部分。

---

## 特殊标签处理

### 需要跳过的节点

```python
# 自闭合标签，不需要翻译
SKIP_TAGS = {
    "<br", "<img", "<input", "<link", "<meta",
    "<area", "<base", "<col", "<embed", "<hr",
    "<source", "<track", "<wbr"
}

def should_skip_node(node):
    if is_self_closing(node.tag):
        return True
    if node.tag in SKIP_TAGS:
        return True
    return False
```

### 术语表

```python
glossaries: dict[str, str] = {
    "Hello": "你好",
    "World": "世界",
    "artificial intelligence": "人工智能",
}
```

---

## 错误处理与重试策略

### 重试机制

```python
MAX_RETRIES = 3
RETRY_DELAY = 2  # 秒

async def translate_chunk_with_retry(chunk: ChunkState) -> ChunkState:
    """带重试的翻译"""
    for attempt in range(MAX_RETRIES):
        try:
            return await translate_chunk(chunk)
        except TranslationError as e:
            if attempt < MAX_RETRIES - 1:
                logger.warning(f"Translation failed (attempt {attempt + 1}), retrying...")
                await asyncio.sleep(RETRY_DELAY * (attempt + 1))
                continue
            else:
                # 所有重试都失败
                chunk.status = ChunkStatus.FAILED
                chunk.error = str(e)
                return chunk
```

### 错误类型与处理

| 错误类型 | 说明 | 处理方式 |
|---------|------|---------|
| `TranslationFormatError` | LLM 返回格式错误 | 重试 |
| `TranslationIncompleteError` | 翻译数量不完整 | 重试 |
| `TranslationEmptyError` | 翻译结果为空 | 重试 |
| `NetworkError` | 网络错误 | 重试 |
| `RateLimitError` | API 限流 | 等待后重试 |
| `TokenLimitExceeded` | Token 超限 | 拆分为更小的 chunk |

### 失败 Chunk 处理

```python
# 统计失败
failed_chunks = [c for c in manifest.chunks if c.status == ChunkStatus.FAILED]

if failed_chunks:
    logger.error(f"{len(failed_chunks)} chunks failed translation:")
    for chunk in failed_chunks:
        logger.error(f"  - {chunk.xpath}: {chunk.error}")

    # 选项 1：继续处理其他 chunk
    # 选项 2：中止并报告
    # 选项 3：支持手动重置失败 chunk
```

### 手动重置机制

```python
def reset_failed_chunks(manifest: TranslationManifest) -> TranslationManifest:
    """将所有 FAILED 状态的 chunk 重置为 PENDING，支持重新翻译"""
    for chunk in manifest.chunks:
        if chunk.status == ChunkStatus.FAILED:
            chunk.status = ChunkStatus.PENDING
            chunk.error = None
    return manifest
```

---

## 实施计划

### 阶段划分与依赖

| 阶段 | 任务 | 依赖 | 并行 |
|------|------|------|------|
| **Phase 1: 基础设施** | | | |
| 1.1 | 实现 `engine/item/tree.py`（TreeNode） | - | ✅ 可并行 |
| 1.2 | 实现 `engine/item/xpath.py`（xpath 解析） | - | ✅ 可并行 |
| 1.3 | 实现 `engine/item/token.py`（Token 估算） | - | ✅ 可并行 |
| **Phase 2: 核心分块** | | | |
| 2.1 | 实现递归分块算法 | 1.1, 1.2, 1.3 | |
| 2.2 | 实现 Nav/Toc 特殊处理 | 2.1 | |
| **Phase 3: 持久化** | | | |
| 3.1 | 实现 `engine/item/manifest.py` | 2.1 | |
| **Phase 4: 工作流** | | | |
| 4.1 | 修改 `engine/schemas/chunk.py` | 1.1 | |
| 4.2 | 修改 `engine/agents/workflow.py` | 3.1, 4.1 | |
| 4.3 | 修改 `engine/orchestrator.py` | 4.2 | |
| **Phase 5: 测试** | | | |
| 5.1 | 单元测试 | 1-4 | |
| 5.2 | 集成测试 | 5.1 | |

### 并行执行策略

- **Phase 1**: 3 个任务完全独立，可并行执行 3 个 subagent
- **Phase 2**: 2.1 先执行，2.2 依赖 2.1
- **Phase 3**: 独立，在 2.1 后执行
- **Phase 4**: 顺序执行，4.1 → 4.2 → 4.3
- **Phase 5**: 可并行

---

## 验收标准

1. **结构完整性**：每个 Chunk 都是完整的 HTML 子树，标签永不配对丢失
2. **Token 限制**：每个 Chunk 的 token 数不超过 SAFE_LIMIT
3. **断点续传**：翻译中断后，重新启动只重新翻译未完成的 Chunk
4. **上下文连贯**：Chunk 边界处有上下文传递
5. **位置记录准确**：xpath 位置信息准确，用于验证翻译后结构
6. **Nav/Toc 支持**：导航文件使用相同架构处理
7. **术语表应用**：翻译时正确应用术语表

---

## 保留/修改清单

### 完全保留（无需改动）

| 文件/组件 | 原因 |
|-----------|------|
| `engine/agents/translator.py` | Agent 框架不变，prompt 不需要修改 |
| `engine/agents/proofer.py` | Agent 框架不变，prompt 不需要修改 |
| `engine/agents/models.py` | 模型配置 |
| `engine/core/logger.py` | 日志系统 |
| `engine/services/glossary.py` | 术语表提取和加载 |
| `engine/schemas/translator.py` | TranslationStatus 枚举 |
| `engine/epub/parser.py` | EPUB 解析 |
| `engine/epub/builder.py` | EPUB 构建 |
| `engine/epub/replacer.py` | HTML 替换恢复 |

### 需要修改

| 文件/组件 | 修改内容 |
|-----------|---------|
| `engine/schemas/chunk.py` | 新增 `xpath`, `text_positions`, `context_before`, `context_after` 字段 |
| `engine/agents/workflow.py` | 改为新流程：chunk → translate → validate → merge |
| `engine/orchestrator.py` | 适配新的 chunk 结构和工作流 |
| `engine/item/chunker.py` | 重写为树状递归分块算法 |
| `engine/item/placeholder.py` | 改用 xpath 位置记录替代占位符 |
| `engine/agents/validator.py` | `validate_placeholders` 改为 HTML 标签验证 |

### 需要新增

| 文件 | 说明 |
|------|------|
| `engine/item/xpath.py` | xpath 解析和校验 |
| `engine/item/token.py` | Token 估算（从 chunker.py 提取并增强） |
| `engine/item/tree.py` | HTML 树状解析器（TreeNode） |
| `engine/item/manifest.py` | Manifest 持久化管理 |

### 需要删除

| 文件 | 原因 |
|------|------|
| `engine/item/renumberer.py` | 不需要 |
| `engine/agents/verifier.py` | 验证逻辑重写 |
| `engine/agents/html_validator.py` | HTML 验证逻辑重写 |

### 新旧架构对比

| 方面 | 当前架构 | 新架构 |
|------|---------|--------|
| 分块方式 | HTML 标签分割 + 占位符 | 树状递归 + 贪心合并 |
| 位置记录 | 无 | xpath 绝对路径 |
| 验证方式 | 占位符数量验证 | HTML 标签配对验证 |
| Chunk 数据 | `local_tag_map` | `text_positions` (xpath 列表) |
| 持久化 | save_json (chunk 级别) | Manifest (文档级别) |
| 断点续传 | 按 chunk 保存 | 按文档+chunk 保存 |
| 翻译方式 | 整段文本含占位符 | 整段文本含标签 |

### 迁移策略

1. **渐进式迁移**：新架构作为并行实现，验证通过后逐步替换
2. **保留旧架构**：旧代码保留，确认新架构稳定后删除
3. **数据兼容**：Manifest 格式独立版本管理
